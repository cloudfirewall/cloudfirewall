# Engine

The engine app owns policy validation, compilation, simulation, and artifact generation workflows.

- CLI entrypoint: `cmd/engine-cli`
- Engine internals: `internal/policy` and `internal/platform`
