# API

This directory is reserved for API-facing code. `types/` holds transport-oriented DTOs that stay decoupled from `apps/engine/internal`, so the API app can evolve independently from engine implementation details.
